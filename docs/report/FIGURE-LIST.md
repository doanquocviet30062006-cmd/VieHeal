# Figure and table plan

Planned figures: current backend architecture; target production architecture; domain ERD; encounter state/workflow; Android module dependency graph; UDF data flow; navigation graph; OIDC PKCE sequence; Room/network synchronization; Figma main flow; API/database trace; AI gateway sequence; AI safety pipeline; test pyramid; deployment topology; Git history; roadmap.

Planned tables: capability status; requirements; endpoint/permission/error contract; migration catalog; database/application enforcement; mobile screen/state matrix; Room entities/TTL; error mapping; AI scenarios/results; rubric matrix; prioritized gaps.

Every figure gets a number, descriptive caption, source (`Author, derived from repository commit …` where appropriate), readable labels and in-text discussion. Do not label a target diagram as deployed architecture.
